#include "../../include/database/Constraint.h"


// void PrimaryKeyConstraint::check(const std::string& value) const {
//     // Should be enforced at the table level
// }

// void ForeignKeyConstraint::check(const std::string& value) const {
//     // Should be enforced at the table level
// }
